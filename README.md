# 🐝 BeeClaimer Pro V2 - Telegram Mini App

**BeeClaimer Pro** هو تطبيق مصغر (Mini App) متطور لمنصة تليجرام، متخصص في توزيع العملات الرقمية (TON) من خلال نظام "الصنبور" (Faucet) والتعدين السحابي المحاكي (Cloud Mining)، مع لوحة تحكم إدارية مخفية.

---

## 🚀 المميزات الرئيسية (Core Features)

*   **💎 نظام الصنبور الذكي (Faucet System):** مطالبة بالعملات كل 6 ساعات مع عد تنازلي حي وشريط تقدم.
*   **⛏️ التعدين السحابي (Cloud Mining):** نظام تعدين متطور بواجهة تفاعلية تعرض معدل الهاش (Hashrate) والأرباح المباشرة بالثانية.
*   **🏦 المحفظة الاحترافية (Professional Wallet):** سجل معاملات كامل، مع نظام سحب يدعم شبكة TON.
*   **👥 نظام الإحالة (Referral System):** رابط إحالة فريد لكل مستخدم مع تتبع فوري للأصدقاء النشطين.
*   **📢 دمج Adsgram:** تفعيل الإعلانات الإجبارية قبل المطالبة لضمان ربحية صاحب البوت.
*   **🔐 لوحة تحكم سرية (Secret Admin Panel):** الوصول للوحة التحكم عبر الضغط **8 مرات متتالية** على أيقونة مخفية في الهيدر، محمية بكلمة مرور.

---

## 🛠️ التكنولوجيا المستخدمة (Tech Stack)

*   **Frontend:** React.js, TypeScript, Tailwind CSS.
*   **Backend/DB:** Firebase Firestore (NoSQL).
*   **Animations:** CSS Orbitals & Keyframes.
*   **Ads:** Adsgram SDK Integration.
*   **Hosting:** Vercel / GitHub Pages.

---

## 📂 هيكل المشروع (Project Structure)

```text
├── components/           # المكونات مثل دائرة التعدين وبطاقات الإحصائيات
├── services/             # خدمات الربط مع Firebase و Gemini API
├── App.tsx               # الملف الرئيسي الذي يحتوي على منطق التطبيق والواجهة
├── index.tsx             # نقطة الدخول (Entry Point) لتطبيق React
├── types.ts              # تعريفات الأنواع (Interfaces) للبيانات
├── index.html            # ملف HTML الأساسي مع CDN اللازمة
├── metadata.json         # بيانات وصفية للتطبيق وصلاحيات الإطار
└── package.json          # تعريف المكتبات والاعتمادات (يُنشأ عند الرفع)
```

---

## ⚙️ طريقة التشغيل (Setup Instructions)

### 1. إعداد قاعدة البيانات (Firebase)
1. قم بإنشاء مشروع جديد في [Firebase Console](https://console.firebase.google.com/).
2. فعل خدمة **Firestore Database**.
3. قم بإنشاء Collection باسم `settings` وداخلها Document باسم `global` للتحكم في الإعدادات.
4. قم بتحديث بيانات `firebaseConfig` في ملف `App.tsx` ببيانات مشروعك الخاصة.

### 2. الرفع (Deployment)
*   قم برفع الملفات إلى مستودع GitHub.
*   اربط المستودع بمنصة **Vercel**.
*   تأكد من إعداد رابط الـ API الخاص بـ Gemini في المتغيرات البيئية إذا لزم الأمر.

### 3. الربط مع تليجرام
*   من خلال `@BotFather` أنشئ بوت جديد.
*   استخدم أمر `/newapp` واربطه برابط الـ URL الخاص بالتطبيق المرفوع.

---

## 🛡️ الدخول للوحة التحكم (Admin Access)
للدخول إلى الإعدادات وتغيير المكافآت أو الموافقة على السحوبات:
1. اذهب للهيدر (أعلى التطبيق).
2. اضغط في المنطقة الفارغة بجانب أيقونة "BeeClaimer" **8 ضغطات سريعة متتالية**.
3. ادخل كلمة المرور الافتراضية: `1234` (يمكنك تغييرها لاحقاً من لوحة التحكم نفسها).

---

**تم إعداد الكود ليكون نسخة نهائية معتمدة وجاهزة للإنتاج.**
